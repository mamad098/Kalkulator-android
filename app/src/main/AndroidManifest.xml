<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/gambar_kalkulator"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gambar_kalkulator_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kalkulator">
        <activity
            android:name=".activity_keempat"
            android:exported="false" />
        <activity
            android:name=".Activity_Ketiga"
            android:exported="false" />
        <activity
            android:name=".halaman_utama"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Kalkulator"
            android:exported="false" />
    </application>

</manifest>